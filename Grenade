using UnityEngine;
using System.Collections;
using UnityEngine.UI;

public class Grenade : MonoBehaviour {

	public gameobject Grenade_Object;
	public float hold_time;
	Public text frag_amount;
	Public float grenade_type;
  
	/*void Start () {
	
	}*/
	
	// Update is called once per frame
	void Update() {
  if (Input.GetKeyDown("h"))
  {
  startcouratine(Hold())
  }
	}
	
	Ienumerator Hold ()
	{
	if (type = 4)
	{
	Holding = true;
	Impact = true;
	}
	}
}
